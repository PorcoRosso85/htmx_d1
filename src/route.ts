/**
 * TODO: workerでは[site]bucket="./assets"で静的ファイルを提供する
 * トップページとsw.tsを提供し、sw.tsにフロント側のstyle, scriptをキャッシュさせる
 * 実装では x c.render(), o c.html()、htmlのみを提供すればいいようにする
 *
 * このファイルでは、静的ファイルの提供
 * swの実行確認
 * style, scriptが適用されているかの確認を行う
 */
